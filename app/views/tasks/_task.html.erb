<%= div_for task do %>
  <%= task.description %>
  <%= check_box_tag(:task, value = nil, checked = task.done) %>
  <%= task.date.to_formatted_s(:short) %>
  <div class="actions">
    <%= link_to 'Edit', edit_task_path(task), class: :edit, remote: true %>
    <%= link_to 'Destroy', task, :method => :delete, :confirm => 'Are you sure?', remote: true %>
  </div>
<% end %>

  
